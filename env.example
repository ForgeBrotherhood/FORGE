# Root env example for the FORGE monorepo.
# Copy the relevant values into:
#   - api/.env        (backend)
#   - mobile/.env     (mobile app)

########################
# Shared / general
########################
NODE_ENV=development

########################
# API (Node / Express)
########################
API_PORT=4000

# Database (example: Postgres)
DATABASE_URL=postgresql://forge_user:forge_password@localhost:5432/forge

# Auth
JWT_SECRET=super-secret-jwt-key-change-me

# OpenAI
OPENAI_API_KEY=sk-your-openai-key-here

# Stripe (or other billing)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

########################
# Mobile (Expo / React Native)
########################

# Base URL the app uses to talk to your API.
# In dev, this might be your local machine IP (or ngrok URL).
EXPO_PUBLIC_API_BASE_URL=http://localhost:4000

# Optional analytics / error tracking
EXPO_PUBLIC_SENTRY_DSN=

# If you use Stripe in the app
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key
