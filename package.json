{
  "name": "forge-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "FORGE – Brotherhood + The Smith (mobile app + backend) monorepo",
  "scripts": {
    "dev": "turbo run dev",
    "dev:mobile": "pnpm --filter mobile dev",
    "dev:api": "pnpm --filter api dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "workspaces": [
    "mobile",
    "api",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
